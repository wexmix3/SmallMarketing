{
  "version": 2,
  "buildCommand": "if [ -f build.sh ]; then chmod +x build.sh && ./build.sh; else build.cmd; fi",
  "installCommand": "npm install",
  "framework": null,
  "outputDirectory": ".next",
  "regions": ["iad1"],
  "env": {
    "NEXT_PUBLIC_API_URL": "https://api.marketpro.com",
    "NODE_ENV": "production"
  }
}
